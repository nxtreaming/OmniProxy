# v1.1.1-beta.5 - quota card layout polish / 额度卡片布局优化

This beta polishes the quota page for API Key accounts. It gives balance and proxy-usage metrics more room and improves token usage readability in dense provider views.
本次 beta 优化额度页面的 API Key 账号卡片展示：余额和代理请求指标会占满可用空间，Token 用量在密集账号视图中更容易阅读。

## Features / 新特性

- None / 无。

## Bug Fixes / 问题修复

- **Quota Card Layout**: API Key quota cards now use a dedicated two-column metrics layout when no subscription quota windows are present.
  - **额度卡片布局**: API Key 账号在没有订阅额度窗口时，改用专门的双列指标布局。
- **Token Usage Readability**: Proxy token usage is split into separate `Token`, `入`, and `出` metrics instead of one squeezed inline string.
  - **Token 用量可读性**: 代理 Token 用量拆分为 `Token`、`入`、`出` 三个指标，避免被窄卡片挤压。

## Breaking Changes / 不兼容更新

- **Attention**: No backend API or local data format breaking changes are expected in this beta release.
  - **必读**: 本 beta 版本预计没有后端 API 或本地数据格式不兼容更新。

## Refactor & Performance / 重构与优化

- **Responsive Layout**: Added responsive handling for the new API-only quota card layout.
  - **响应式布局**: 为新的 API-only 额度卡片布局补充窄屏降级处理。

## Validation / 验证

- `cmd /c npm test`
- `cmd /c npm run build`

---

**Full Changelog / 完整更新日志**: https://github.com/mibgb65-cloud/OmniProxy/compare/v1.1.1-beta.4...v1.1.1-beta.5
